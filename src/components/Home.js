import React from 'react';
import { Grid } from 'react-bootstrap';

const Home = () => {
    return (
        <Grid>
            <h1>Welcome to Camp Wisconsin!</h1>
        </Grid>
    );
};

export default Home;
